portfolio:
  name: <Name your portfolio>

jira:
  server: https://sweep.atlassian.net/
  username: <Jira username>
  token: <Get token from Jira>

database:
  filename: sweep.db
  path: <Enter path>

loading:
  mode: {jira|mixed|database}

reports:
  # templatedir: <directory in which templates are placed, default is loading templates from package service.web>
  directory: <your directory for writing generated reports>

plugins:
  sections:
    HelloWorldPlugin:
      title: Hi there!
    <classname of Plugin A>:
      title: <title of chart A>
    <classname of Plugin B>:
      title: <title of chart B>